{
  "name": "miniforecast",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based demand forecasting tool — 8 statistical models, zero backend",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "node scripts/serve.mjs"
  },
  "dependencies": {
    "arima": "^0.2.8",
    "chart.js": "^4.4.1",
    "esbuild": "^0.28.2",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
